/** Shared horizontal padding / max width for store pages. */
export const STOREFRONT_SHELL = 'mx-auto w-full max-w-[96rem] px-4 sm:px-6 lg:px-10'

export const DEFAULT_APP_SHELL = 'mx-auto w-full max-w-[96rem] px-4 sm:px-6 lg:px-10'

/** Account / profile: use the full viewport width (no centered max-width column). */
export const FULL_WIDTH_APP_SHELL = 'w-full px-4 sm:px-6 lg:px-8 xl:px-10'

/** Marketing / landing: edge-to-edge main (header keeps its own shell). */
export const FLUSH_APP_SHELL = 'w-full'
