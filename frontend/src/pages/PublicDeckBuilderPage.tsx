import CommanderSynergyPage from './CommanderSynergyPage'

/** Public catalog deck builder — no store slug or inventory required. */
export default function PublicDeckBuilderPage() {
  return <CommanderSynergyPage variant="public" />
}
